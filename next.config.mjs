/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "ytaxhdbtcofsuc9z.public.blob.vercel-storage.com",
    ],
  },
};

export default nextConfig;
